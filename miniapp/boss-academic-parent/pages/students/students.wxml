<view class="page">
  <view class="top">
    <view class="eyebrow">我的</view>
    <view class="title">选择学生</view>
    <view class="subtitle">切换后，首页、服务、课表和财务会显示对应信息。</view>
  </view>

  <block wx:if="{{students.length}}">
    <view wx:for="{{students}}" wx:key="id" class="card" bindtap="selectStudent" data-index="{{index}}">
      <view class="row">
        <view>
          <view class="value">{{item.name}}</view>
          <view class="label">{{item.school || "学校未填写"}} · {{item.grade || "年级未填写"}}</view>
        </view>
        <view class="pill" wx:if="{{item.id === currentStudentId}}">当前</view>
      </view>
      <view class="subtitle">{{item.servicePlanLabel || "普通课程"}} · 查看学习与服务进展</view>
    </view>
  </block>
  <view wx:else class="empty">还没有绑定学生</view>

  <button class="btn secondary" bindtap="goBind">绑定新学生</button>
  <button class="portal-foot-link" loading="{{loggingOut}}" disabled="{{loggingOut}}" bindtap="logout">退出家长登录</button>
</view>
