# AI 子域名切换

老板授权主域名 gtaisg.com 用于公司官网，AI 平台改用 ai.gtaisg.com。

仅替换正式登录回调和 AI 方案客户端的默认上游地址。原会话、角色、凭证签名及内部服务调用机制不变。先启用新域名证书与原 4310 上游，再发布回调修正，最后切主域名。

## 员工操作影响

仍使用原系统账号登录，改用新的 AI 地址；无新增输入、按钮或业务决定。

## 界面复杂度核对

无员工界面变更。

## 验证

本地 259 页生产构建通过，待标准发布门禁。公开 AI API 未登录必须维持拒绝，网页应转正式登录；只做只读会话验收，不执行 AI 业务写入。
