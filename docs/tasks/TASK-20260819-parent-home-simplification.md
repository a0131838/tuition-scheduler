# TASK-20260819 家长端首页二次精简与服务入口补齐

## 目标

把家长端首页从“信息报告页”改为“行动入口页”，同时避免过度精简后形成大面积空白。家长打开后先判断是否有事情要处理，再看到下一节课、真实进展和高频服务入口。

## 本次范围

- 首页顶部只保留学生姓名、服务类型和切换孩子入口。
- 同一时间最多展示一个需要家长处理的事项。
- 仅在存在已确认课程时展示下一节课；不再展示空课表说明。
- 删除与底部导航重复的“课表、服务进展”入口。
- 只在存在真实课后反馈、照护更新或服务时间线时展示一条最新进展；没有真实数据时不生成占位内容。
- 增加四个紧凑服务入口：新加坡学校、入学测评、提交需求（无权限时为我的资料）和联系顾问。
- 移除首页的大型状态说明、进展长文和本周数据；完整数据仍在进展、课表和我的页面中。

## 安全边界

- 只修改家长原生小程序首页展示、页面路由方法和对应回归测试。
- 不修改 API、数据库、登录、权限、课程、工单、课包、通知、考勤、财务或排课逻辑。
- 本轮补充修改只在独立工作树验证；未经老板再次确认不上传、不提交审核、不正式发布。

## 验收

- 首页首屏不再出现进展长文、空课表说明和周数据。
- 有待办时只展示一个主要待办；无待办时只显示一行安全状态。
- 有下一节课才显示课程区块。
- 最新进展只展示真实数据，最多两行摘要。
- 四个高频服务入口可点击，工单入口继续遵守原有权限判断。
- 聚焦回归、原生 JavaScript 语法和生产构建通过。

## 2026-08-19 补充验收

- `node --check miniapp/boss-academic-parent/pages/home/home.js` 通过。
- 家长首页与信息架构定向测试共 `13/13` 通过。
- Next.js 生产构建完成 `251/251` 页面，退出码 `0`。
- `git diff --check` 通过；未修改 API、数据库或正式业务逻辑。
- 微信开发体验版 `1.0.63` 已于 2026-08-19 16:40（Asia/Singapore）上传成功；未提交审核、未正式发布。

## 发布结果

- 服务器已运行功能提交 `4886bf4b551dd3978893e3fcce61e527a65081c3`，PM2 PID `3455329`，健康检查 HTTP `200`。
- 微信开发体验版 `1.0.62` 已于 2026-08-19 15:44（Asia/Singapore）上传，包体 780.4 KB。
- 本轮家长首页服务入口补齐版本 `1.0.63` 已于 2026-08-19 16:40（Asia/Singapore）上传成功。
- 未提交微信审核，也未正式发布；正式线上小程序不会因此自动变化。
